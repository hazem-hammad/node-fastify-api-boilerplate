import database from "core/database/database";

export { Connection, default as connection } from "core/database/connection";
export { Database } from "core/database/database";
export default database;
